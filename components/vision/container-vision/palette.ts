/* Container Vision uses the shared vision-scene palette. Kept as a re-export so
   this module's many local importers don't all have to change, and so a future
   scene-specific override has an obvious home. */
export { PALETTE, sans } from "../_vision/palette";
